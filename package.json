{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "A simple weather app using asynchronus HTTP calls",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rahul Bera",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.0"
  }
}
